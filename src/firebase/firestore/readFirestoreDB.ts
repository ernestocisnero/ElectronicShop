

export const readFirestoreDB = ()=>{
    // const querySnapshot = await getDoc(doc(firestoreCartDB, "user_carts", "USER_ID_fdkfiklonvb8676482"));

    // const data = querySnapshot.data();
    // data?.cart_items.map((item: any) => {
    //     console.log(item);
    //     console.log("Product ID", item.productID, "Count", item.count);
    // })
}