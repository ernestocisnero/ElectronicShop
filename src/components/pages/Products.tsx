import { PublicityBanner } from '../UI/PublicityBanner';

export const Products = (): JSX.Element => {

    return (
        <>
            <PublicityBanner />
            <h1>Products</h1>
        </>
    )
}
